// In clientRoutes.js
const express = require('express');
const router = express.Router();

// Route to retrieve client details
router.get('/retrieve-details', (req, res) => {
    // Handle logic to retrieve client details
});

module.exports = router;
